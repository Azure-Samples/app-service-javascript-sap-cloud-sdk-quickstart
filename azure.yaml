# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: app-service-javascript-sap-cloud-sdk-quickstart
metadata:
  template: app-service-javascript-sap-cloud-sdk-quickstart
services:
  sap-cloud-sdk-api:
    project: ./src/api
    language: ts
    host: appservice
    hooks:
      predeploy:
        posix:
          shell: sh
          run: ../../hooks/predeploy/testexecution.sh
          continueOnError: false
          interactive: true
        windows:
          shell: pwsh
          run: ./hooks/predeploy/testexecution.ps1
          continueOnError: false
          interactive: true
# Remove the comments from the lines below to enable the deployment of the infrastructure via Terraform
#infra:
#  provider: terraform
#  path: ./infra-terraform
